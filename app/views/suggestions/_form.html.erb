<%= form_for(@suggestion) do |f| %>
  <% if @suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description, "Keyword" %><br>
    <%= f.text_area :description %>
  </div>
  <div>
    <%= f.label :answers %>
    <%= collection_select(:suggestion, :answer_ids, Answer.all, :id, :description, {}, { :multiple => true } )%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
